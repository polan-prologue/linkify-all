# Changelog

## 1.1.0

### 修复

- 修复：评论中的链接被截断成半截链、无法跳转。平台在渲染评论时会把链接文字
  当成普通文本流做词汇切分，URL 中段可能被单独包成站内链接——实测案例中
  链接 `https://app.example.com/register?referrer_code=abc123` 被拆成三段：
  `…/register?r` ＋ 站内搜索链接 `eferrer_code` ＋ `=abc123`，切点恰好落在
  字母数字中间。此前的跨节点续接（1.0.2 / 1.0.7）假设"切点只落在 URL 结构
  分隔符（`://`、`.`、`/`）上"，字母数字收尾的命中一律不续接，于是半截链
  `…/register?r` 被当成完整链接直接转换，点击后跳到错误地址
- 修复（根本性）：续接判定不再按"切点长什么样"猜，改为统一原则——
  **续接绝不回改已识别的主机**。命中在主机之后已含路径/查询分隔符（`/` `?` `#`）
  时，续接只能延伸路径/查询、不可能污染主机，因此字母数字收尾也放行续接；
  命中止于主机末尾（无路径）时续接必然把主机和正文单词粘连
  （`example.com` ＋ `Uninstaller` 型），保持保守不吞。至此覆盖全部切点形态：
  主机残缺、结构分隔符上、路径/查询字符流内

## 1.0.9

### 修复

- 修复：设置窗口可叠加多个。菜单里连点「⚙️ 设置…」每次都会新开一个设置面板，
  屏幕上叠出多份。现在设置面板为单例——已打开时再点菜单只把已有面板提到最前，
  并提示「设置面板已打开」；「🎓 学习新规则」弹窗（菜单 + 设置面板内按钮两个入口）
  同理处理
- 精简：学习规则空状态文案「还没有学习规则。点上方「＋ 学习新规则」输入一对示例
  即可教会它。」改为「暂无规则，点上方「＋ 学习新规则」添加」，单行显示不再折行

## 1.0.8

本版本为**识别正确性 + 安全 + 健壮性**的集中修复版。功能行为变更见下，其余为内部健壮性优化。

### 正确性（此前长期存在的基础识别问题）

- 修复：无协议的下载直链被整条否决。`github.com/.../releases/download/1.0/app.apk`、
  `dl.example.com/setup_v2.exe`、`mirror.example.com/ubuntu-24.04.iso` 这类明文地址
  因为末段扩展名命中文件扩展名黑名单而被当成"文件名"跳过。现在**含路径分隔符的
  候选一律不做文件名否决**——文件名否决本只为拦截无路径的裸附件名
  （`example-app-1.0.17-win64.zip`），那些样本都不含 `/`，此改动无回归
- 修复：三段非域名文本被误转成链接。TLD 判定原为 `[a-zA-Z]{2,24}`，语义等于"任意
  字母串"，`Chrome.120.Release`、`Build.Number.Final` 这类版本串会被转成
  `https://Chrome.120.Release`。现在引入**公共后缀白名单**（ccTLD + 主流 gTLD +
  RFC 2606 保留域）：无协议、无 `www`、无路径的三段主机必须命中白名单才转换；
  带协议头或 `www` 前缀属强信号，不受白名单限制

### 安全

- 修复：导入链路可注入 `javascript:` 伪协议锚点。UI 教学路径（`inferRule`）原本
  有协议校验，但「导入备份」与「从存储加载」两条路径完全绕过校验，被污染的备份
  文件可以让脚本在页面上生成 `javascript:` 链接。现在统一走 `safeRuleUrl` 校验，
  并在 `buildAnchor` 出口加兜底闸门；被拒的命中把原文原样放回，绝不吞页面文本

### 健壮性

- 修复：Shadow DOM 内的按钮 / 编辑器文本不被跳过。祖先链判定遇到 shadow root
  直接 `break`，拿不到宿主，宿主若是 `<button>` 或 `contentEditable`，其内部文本
  会被误转换（富文本编辑器开关在 Web Component 内失效）。现在穿透到宿主继续判定
- 修复：首扫/兜底扫描超预算时 Shadow 宿主被直接丢弃。此前超预算只把剩余文本节点
  入队，`hosts` 数组丢弃，大页面下 Web Component 内的链接可能长时间不转换
- 修复：脚本把自己的设置面板当页面内容转换。面板规则列表里显示的 `sampleUrl`
  会被本脚本转成链接。现在面板带 `data-lfa-ui` 标记，扫描与观察器双重跳过
- 修复：Esc 会一次关掉所有叠加面板（每个面板都注册了 document 级 capture keydown）。
  现在维护面板栈，只有栈顶响应 Esc
- 修复：多标签页状态不同步。A 页关总开关或拉黑站点后 B 页仍在转换。现在监听
  Tampermonkey 存储变更，仅响应其它标签页的写入
- 修复：Mac 上直链跳转不可用。判定写死 `e.ctrlKey && !e.metaKey`，而 Mac 用户
  的自然手势是 Cmd+点击。现在 `Ctrl` / `Cmd` 均可
- 移除无效机制：`internalWrite` 标志在同步块内置位再复位，而 MutationObserver 回调
  在微任务触发，执行时标志早已复位——实际从未生效过，真正起作用的是 `ownNodes`
  WeakSet。已删除该标志及其 4 处引用，避免误导后续维护
- 黑名单支持**子域继承**：加入 `example.com` 即覆盖 `www.example.com`（此前需加两次）
- 失效检测跳过范围扩大：补充保留域（`.local/.internal/.lan/...`）与 IPv6 环回 / ULA，
  不再对内网地址做无意义的匿名探测

### 内部改进

- 版本号收敛为单一常量 `VERSION`（此前硬编码在 3 处）
- 删除死代码 `convertSelection()`（定义了但全项目无任何调用点）
- 回归测试扩充至 **124 项**（新增 Q 组 14 项，覆盖下载直链 / 公共后缀白名单正反例 /
  伪协议防护 / 面板免疫 / Shadow 宿主穿透 / 黑名单子域继承等本次修复点），全部通过

## 1.0.7

- 修复：内容平台把链接文本**沿 URL 结构边界切开**时（把链接里的关键词包成
  站内搜索锚插在链接中段），若截断片段恰好是「完整长相」——主机名完整
  （如顶级域恰为 2 字符）或切点落在路径 `/`、主机 `.` 上——此前的续接触发器
  判定主机不残缺而不启动，截断的半截地址会直接转成错误链接
  （如深链只链到站点首页）
- 跨节点续接触发器补全为**覆盖全部结构性切点**（与既有能力合并）：
  - 协议尾 `://` 切分（v1.0.4 已覆盖）
  - 主机中段切分、主机残缺（v1.0.2 已覆盖）
  - **主机点号 `.` 边界切分（本次新增）**
  - **路径斜杠 `/` 边界切分（本次新增）**：原匹配以 URL 结构符（非字母数字）
    收尾、且紧邻内联元素（平台搜索锚 / span 包裹）以 URL 字符开头时，
    同样进入续接管线组装完整链接并吞并平台锚点
- 防误吞护栏保持保守：纯文本行文兄弟（无平台包裹）不吞、命中以字母数字
  收尾（无结构边界）不吞、组装结果仍过主机完整性与 URL 合法性双重闸门，
  组装不成宁可不转（宁缺毋错）
- 新增 P 组通用回归测试 12 项（真实拆分拓扑的站名无关复刻、点号/斜杠边界、
  span 包裹、纯文本负例、字母数字收尾负例、全文零丢失不变量），
  全量 109 项断言通过

## 1.0.6

- 性能优化（功能行为不变，聚焦「页面开销最小化、浏览器不卡」）：
  - 场景开关与总开关状态改为内存快照：扫描每个文本节点时不再同步读 GM 存储
    （同步 IPC 往返，大页面首扫/链接密集页是主要卡顿来源之一）
  - MutationObserver 回调改为「整批收集 → 祖先去重 → 入队」：同批同时新增
    「父容器 + 子节点」（或子节点挂在已待处理祖先下）时只处理最外层根，
    不再对重叠子树反复爬取，框架批量插大树明显提速
  - 本脚本转换产生的锚点/文本登记进 WeakSet：自身 DOM 写入不再回流观察器队列，
    即使开启「已有链接内部」也不会对自己转换的链接重复转换（防嵌套）
  - 提取码搜索只对网盘链接执行：非网盘命中不再做链接周边文本的补充正则搜索
  - 预筛快速门：不含 `.` 且不含 `:` 的文本（纯中文/纯文本节点）跳过完整预筛正则，
    只查学习规则探测
  - 扫描预算制：初始全页扫描单轮 ≤20ms、兜底扫描单轮 ≤6ms，超预算的剩余节点
    转入既有分块队列（每批 8 个 + 宏任务让出）——超大页面也不会出现单次长任务
    阻塞渲染；兜底扫描被预算中断时保持密集档位继续扫，不误降频
  - 兜底扫描对已转换链接内的文本直接拒绝（免祖先链判定与预筛），
    链接密集页面的周期扫描开销大幅下降
  - 设置/学习面板关闭时移除全局 mousemove/mouseup 拖拽监听
    （此前每次打开面板都会在 document 上遗留两个监听器）

## 1.0.5

- 新功能：**Ctrl + 点击**明文网址文本直接跳转目标链接（包含被防误判逻辑
  跳过的裸域名、文件名形态文本），与 Alt+点击复制原文互不冲突
- 性能优化（功能行为不变）：
  - `processRootDeep` 改为单次 TreeWalker 遍历同时收集文本节点与 Shadow DOM 宿主，
    避免重复爬树，显著降低大页面的扫描耗时
  - 遍历时跳过已脱离文档的节点（`!isConnected`），不再处理无效 DOM 子树
  - 规则探测改为合并单一正则，替代逐规则遍历字符串搜索；规则增删时自动重建
  - 预筛正则合并为一个，减少正则调用次数
  - 失效检测 pump 从固定 600ms 改为自适应间隔：有任务时 600ms 密集轮询，
    队列耗尽时逐级降频至 5s，有新任务时立即唤醒
  - MutationObserver 队列分块处理（每批 8 节点），避免大变更一次性阻塞主线程
  - 零宽字符剥离时标记 `internalWrite`，避免触发多余的 MutationObserver 回调
  - 页面恢复可见时立即唤醒失效检测

## 1.0.4

- 修复：链接文本被内容平台拆进多个内联节点时，若首个节点恰好只剩裸协议头
  （`https://` 后面零个字符），常规匹配在该节点必然落空、跨节点续接器没有
  启动种子，整条链接完全不被识别。现在为这类「协议尾」合成种子命中，交给
  同一套跨节点续接管线按既有验证规则组装——合法性闸门不变，续接失败依然
  宁缺毋错。修复在模式层面通用：任意域名、多枚平台局部锚点交错、中文文本
  与链接粘连等全部同形拆分均覆盖，不针对任何具体站点
- 续接器可串联的兄弟节点上限 6→12，适配更深的平台拆分链

## 1.0.3

- 性能优化（功能行为不变）：
  - 兜底扫描按页面变更门控：自上轮扫描后毫无变更的轮次直接跳过（保留连续
    4 轮强制保底，防观察器失效）；零新转换的轮次自动降频 2.5s→5s→10s→30s，
    一旦扫出新转换立即恢复密集保险
  - 扫描本体移到浏览器空闲时段执行（requestIdleCallback，2 秒兜底），
    不再挤占渲染帧
  - 场景排除判定按父元素缓存，重复扫描/兄弟节点密集时不再逐节点爬祖先链
  - 失效检测限流收紧：每页探测上限 50→20、起始延迟 1.2s→2.5s、
    后台标签页不再发请求

## 1.0.2

- 修复：部分内容平台会把链接文本拆进多个相邻内联节点（甚至中间插入平台自带的
  局部链接），单节点匹配只能看到残缺片段，被误转成「https://主机首段」这类错误
  短链。现在遇到节点末尾的不完整主机名时自动跨节点续接后续文本，还原完整链接
  并吞并平台插入的局部锚点；无法续接时宁可不转换，绝不出残缺链
- 完整链接结尾于节点边界时行为不变（不会误吞后续无关文本）

## 1.0.1

- 修复：部分内容平台或复制链路会在网址中段夹带零宽字符（U+200B–U+200F / U+2060 /
  U+FEFF / U+00AD，肉眼不可见），导致链接在字符处被截断，产生「https://主机首段」
  这类残缺短链。现在转换前自动剥离这些不可见字符：显示不变、链接完整还原；
  Alt+点击复制的原文也随之更干净

## 1.0.0 (首个公开版本)

### 核心

- 全站明文网址自动转链接（新标签页打开），带协议 / `www` / 路径 / 子域名地址
  自动补全 `https://`；二段裸域名默认不转（防误判）
- Shadow DOM 穿透 + 周期性兜底扫描：覆盖新版评论区等 Web Component 站点与
  SPA 动态内容
- 场景开关四项：代码块（默认开）/ 富文本编辑器 / 控件文本 / 已有链接内部
- 油猴菜单 4 项 + 统一设置面板，全部改动即时生效（含规则列表即时刷新）

### 学习模式

- 一对示例推导通配规则（固定前缀 + 变量 + 固定后缀），保存前自校验
- 变量按样本泛化（数字 / 字母 / 混合通配，不含 CJK 防吞字）；词边界保护防部分词误匹配
- 同前缀同类型规则自动去重；列表内部滚动 + sticky 添加入口

### 备份

- 本地 JSON 导出/导入（文件选择 + 面板粘贴双通道），按规则内容合并去重
- WebDAV 版本化云备份：时间戳独立存档不覆盖；恢复时 PROPFIND 列出云端版本自由选择；
  目录缺失自动 MKCOL 重试（适配坚果云根目录禁写入）

### 可靠性修复沿袭（自内部版本）

- GitHub Release 附件名误判为网址 → 文件扩展名黑名单否决（`zip/dmg/exe...`），
  带协议的 `.zip` 直链不受影响
- 评论区正文「网址后紧跟全角标点+中文」被吞入链接 → URL 仅匹配可打印 ASCII
- 新版评论区（嵌套 Shadow DOM）不转换 → 根节点自钻取 + 多根监听
- WebDAV 凭据 Basic Auth 支持 UTF-8 密码（原 `btoa` 对非 Latin1 抛异常）

### 安全

- 移除面向页面的调试钩子（改为 URL hash `#lfa-dev` 条件注入，仅供测试）
- 失效检测使用匿名请求（不带 Cookie），限流每页 50 条/并发 2/结果缓存 24h
- 无遥测、无第三方依赖
